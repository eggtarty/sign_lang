# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0

# Core ML / numerical
numpy>=1.23.5
scipy>=1.10.0

# TensorFlow (CPU-only for Render to save space/memory)
# We keep >=2.16.1 to ensure compatibility with your Keras 3 models
tensorflow-cpu>=2.16.1
keras>=3.0.0
h5py>=3.10.0
protobuf>=4.21.0

# MediaPipe 
mediapipe==0.10.9

# OpenCV (Headless is required for server environments)
opencv-python-headless>=4.8.0.0

# Image handling
Pillow>=10.0.0

# API helpers
python-multipart>=0.0.6
python-dotenv>=1.0.0
typing-extensions>=4.6.0

# WebSocket & Server Performance
websockets>=12.0
uvloop>=0.19.0; sys_platform != 'win32'
httptools>=0.6.0
watchfiles>=0.21.0
